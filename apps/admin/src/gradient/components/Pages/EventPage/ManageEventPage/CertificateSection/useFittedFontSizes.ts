/**
 * Moved to `components/Common/Certificate/useFittedFontSizes.ts`.
 * Re-exported so the old import path keeps resolving.
 */
export * from "@/gradient/components/Common/Certificate/useFittedFontSizes";
