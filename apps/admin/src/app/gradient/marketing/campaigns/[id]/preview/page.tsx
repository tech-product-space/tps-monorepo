"use client";

import PreviewPage from "@/gradient/components/Pages/MarketingPage/CampaignEditor/PreviewPage";

// PreviewPage renders DashboardLayout itself so it can put the campaign name
// and a back button in the top bar.
export default function Page() {
    return <PreviewPage />;
}
